#include<stdio.h>
#include<math.h>
int main(){
   int number;
   printf("Enter number :");
   scanf("%d" , &number );
// we use ternary operator.
   number > 0 ? printf("Positive") : number < 0 ? printf("Negative") : printf("Nither positive nor negative ");
   
    return 0;
}
